def narcissistic( value ):
    # Code away
    print(type(value))
    number = str(value)
    number_length = len(number)
    # print( [int(x) ** number_length for x in number])
    # return int(value) == [int(x) ** number_length for x in number]
